/**
 * Seção removida da landing compacta (lojas/legais migraram para SellerFlowFinalCta).
 * Arquivo mantido no repo; não é montado em page.tsx.
 */
export function SellerFlowDownload(_props: {
  googlePlayUrl: string;
  appStoreUrl?: string;
}) {
  return null;
}
